```shell
# jupyter notebook 확장자
.ipynb

# jupyter notebook 에서 pip 명령어 실행시 '%' 를 앞에 붙여야한다.
%pip list
%pip install ...

# jupyter notebook 단축키 in vscode
A : 현재 셀에서 위쪽에 새로운 셀을 추가한다.
B : 현재 셀에서 아래쪽에 새로운 셀을 추가한다.
DD : 현재 셀을 삭제한다.
M : 현재 셀을 코드에서 마크다운으로 변경한다.
Y : 현재 셀을 마크다운에서 코드로 변경한다.
C, V, X : 각각 복사, 붙여넣기, 잘라내기이다.
ctrl + Enter : 현재 셀 실행
```